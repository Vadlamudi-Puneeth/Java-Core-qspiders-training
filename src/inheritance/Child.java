package inheritance;

public class Child extends Parent {
	public static void main(String args[]) {
		// static no need to create an object it means the parent class members are already in this class whenever we use extends keyword
		System.out.println(a);
		demo();
	}
}
